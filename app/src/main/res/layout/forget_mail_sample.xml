<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:padding="20dp"
    android:background="@color/colorWhite"
    android:layout_height="match_parent">
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Forget Password"
        style="@style/TextAppearance.AppCompat.Title"
        android:id="@+id/for_title"/>
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/forget_email_wrapperId"
        android:layout_below="@+id/for_title"
        android:layout_marginTop="20dp"
        android:layout_width="match_parent"
        android:padding="5dp"
        android:background="@drawable/blanck_back"
        android:layout_height="50dp">
        <EditText
            android:id="@+id/for_emailId"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            style="@style/edt1"
            android:inputType="textEmailAddress"
            android:background="@color/colorWhite"
            android:textColor="@color/gray"
            android:hint="Enter your Email"/>
    </com.google.android.material.textfield.TextInputLayout>
    <LinearLayout
        android:layout_marginTop="20dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/forget_email_wrapperId"
        android:orientation="horizontal">
        <Button
            android:id="@+id/for_email_cancelId"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:layout_height="wrap_content"
            android:text="Cancel"
            android:background="@drawable/buttons_primary"
            android:textColor="@color/colorWhite"/>
        <Button
            android:id="@+id/for_email_submitId"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Submit"
            android:layout_marginStart="4dp"
            android:background="@drawable/buttons_primary"
            android:textColor="@color/colorWhite"/>
    </LinearLayout>
</RelativeLayout>